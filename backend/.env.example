# ================================================
# Vikram Solar - Falta EMS Backend Configuration
# ================================================

# -------------------- Server Ports --------------------
# Admin server port for user management and admin authentication
ADMIN_SERVER_PORT=4000

# User server port for user authentication and data access
USER_SERVER_PORT=5000


# -------------------- Security --------------------
# Secret key for JWT token generation (use a strong random string)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Admin registration key (optional - if set, required to create new admin accounts)
ADMIN_REGISTRATION_KEY=your-admin-registration-key


# -------------------- Email Configuration --------------------
# Email service configuration for sending account creation confirmations

# SMTP Host (e.g., smtp.gmail.com, smtp.office365.com, smtp.sendgrid.net)
EMAIL_HOST=smtp.gmail.com

# SMTP Port (587 for TLS, 465 for SSL, 25 for unencrypted)
EMAIL_PORT=587

# Use SSL/TLS (true for port 465, false for port 587)
EMAIL_SECURE=false

# Email account username (usually your email address)
EMAIL_USER=your-email@example.com

# Email account password or app-specific password
# For Gmail: Use an App Password (https://myaccount.google.com/apppasswords)
# For Office365: Use your account password or app password
EMAIL_PASSWORD=your-email-password-or-app-password

# Sender email address (if different from EMAIL_USER)
EMAIL_FROM=noreply@vikramsolar.com

# Sender name displayed in emails
EMAIL_FROM_NAME=Vikram Solar - Falta EMS

# Dashboard URL for login link in emails
DASHBOARD_URL=https://agniswarbanerjee05.github.io/Vikram-Solar-Falta-EMS/


# -------------------- Database --------------------
# SQLite database file path (relative to backend directory)
# Default: ./data/falta-ems.db
# DATABASE_PATH=./data/falta-ems.db


# ================================================
# Email Setup Instructions
# ================================================
#
# Gmail Setup:
# 1. Enable 2-factor authentication on your Google account
# 2. Generate an App Password: https://myaccount.google.com/apppasswords
# 3. Use the generated app password as EMAIL_PASSWORD
# 4. Set EMAIL_HOST=smtp.gmail.com
# 5. Set EMAIL_PORT=587
# 6. Set EMAIL_SECURE=false
#
# Office 365 Setup:
# 1. Use your Office 365 email as EMAIL_USER
# 2. Use your account password as EMAIL_PASSWORD
# 3. Set EMAIL_HOST=smtp.office365.com
# 4. Set EMAIL_PORT=587
# 5. Set EMAIL_SECURE=false
#
# SendGrid Setup:
# 1. Create a SendGrid account and API key
# 2. Set EMAIL_HOST=smtp.sendgrid.net
# 3. Set EMAIL_USER=apikey
# 4. Set EMAIL_PASSWORD=your-sendgrid-api-key
# 5. Set EMAIL_PORT=587
# 6. Set EMAIL_SECURE=false
#
# Custom SMTP:
# 1. Get SMTP credentials from your email provider
# 2. Configure EMAIL_HOST, EMAIL_PORT, EMAIL_USER, EMAIL_PASSWORD
# 3. Set EMAIL_SECURE based on your provider's requirements
#
# Testing Without Email:
# - Simply don't set EMAIL_USER and EMAIL_PASSWORD
# - The system will log "EMAIL DISABLED" messages
# - All other functionality will work normally
#
# ================================================
